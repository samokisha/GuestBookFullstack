# Guest Book Fullstack

## Requirements

- Docker
- docker-compose

## How to run

1. Build
```sh
docker-compose build
```

2. Run MS SQL Server and Api with Vue UI in Docker
```sh
docker-compose up
```

3. Open http://localhost:5000 in browser
4. Explore Vue UI service
